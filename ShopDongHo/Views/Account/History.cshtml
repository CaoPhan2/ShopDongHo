@using ShopDongHo.Models
@model IEnumerable<OrderModel>

@{
    ViewData["Title"] = "Lịch sử đơn hàng";
}

<div class="order-container">

    <!-- HEADER -->
    <div class="order-header">
        <h1>Lịch sử mua hàng</h1>
        <p>Khách hàng: @ViewBag.UserEmail</p>

    </div>

    <!-- TABLE CARD -->
    <div class="order-card">
        <table class="table order-table" id="myTable">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Mã đơn</th>
                    <th>Mã giảm</th>
                    <th>Phí ship</th>
                    <th>Người nhận</th>
                    <th>Ngày</th>
                    <th>Thanh toán</th>
                    <th>Trạng thái</th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>

                        <td class="order-code">
                            #@item.OrderCode
                        </td>

                        <td>
                            @(string.IsNullOrEmpty(item.CouponCode) ? "-" : item.CouponCode)
                        </td>

                        <td>
                            @item.ShippingCost.ToString("#,##0") đ
                        </td>

                        <td>@item.UserName</td>

                        <td>
                            @item.CreateDate.ToString("dd/MM/yyyy")
                        </td>

                        <!-- PAYMENT -->
                        <td>
                            @if (item.PaymentMethod == "COD")
                            {
                                <span class="badge badge-cod">COD</span>
                            }
                            else
                            {
                                <span class="badge badge-online">@item.PaymentMethod</span>
                            }
                        </td>

                        <!-- STATUS -->
                        <td>
                            @if (item.Status == 1)
                            {
                                <span class="badge badge-new">New</span>
                            }
                            else if (item.Status == 2)
                            {
                                <span class="badge badge-done">Finished</span>
                            }
                            else
                            {
                                <span class="badge badge-cancel">Cancelled</span>
                            }
                        </td>

                        <!-- ACTION -->
                        <td>
                            <div class="o-action">
                                @if (item.Status != 3)
                                {
                                    <a class="btn-delete"
                                       onclick="return confirm('Xác nhận hủy đơn hàng?')"
                                       asp-controller="Account"
                                       asp-action="CancelOrder"
                                       asp-route-ordercode="@item.OrderCode">
                                        Hủy
                                    </a>
                                }
                                <a class="btn-view"
                                   asp-controller="Account"
                                   asp-action="ViewOrder"
                                   asp-route-ordercode="@item.OrderCode">
                                    Xem
                                </a>
                            </div>
                            
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
@section Scripts {
    <script>
        let table = new DataTable('#myTable', {
            paging: true,
             lengthChange: false,
            searching: false,
            ordering: false,
            info: false

        });
    </script>
}
<style>
   
</style>